# הגדרות Firebase למשחקים

## שלבים להגדרת Firebase Rules למשחקים:

### 1. כניסה ל-Firebase Console
עבור ל: https://console.firebase.google.com/

### 2. עדכון Firestore Rules
בחר את הפרויקט שלך ועבור ל: **Firestore Database > Rules**

הוסף את הכללים הבאים:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // כל הכללים הקיימים שלך...
    
    // כללים למשחקים - מאפשר לכולם לכתוב וקרוא
    match /gameScores/{scoreId} {
      allow read: if true; // כולם יכולים לקרוא את הטבלת המובילים וההיסטוריה
      allow create: if true; // כולם יכולים לשמור ציונים
      allow update, delete: if false; // לא ניתן לעדכן או למחוק ציונים
    }
  }
}
```

### 3. פרסום הכללים
לחץ על **"Publish"** כדי לשמור את השינויים.

---

## בדיקה
לאחר פרסום הכללים, המשחק אמור לעבוד בצורה חלקה ללא צורך בהתחברות.
המשתמשים יוכלו:
- לשחק במשחק ללא הרשמה
- לבחור שם משתמש
- לראות את טבלת המובילים
- לראות את ההיסטוריה שלהם לפי שם משתמש

---

## תכונות המשחק שהוספו:

### 1. דף משחקים ראשי (`/games`)
- רשימת כל המשחקים הזמינים
- הסברים על כל משחק
- ניווט למשחק, טבלת מובילים והיסטוריה

### 2. משחק אימון אנגלית SQL (`/games/sql-english`)
- משחק של 4 דקות
- 45 מילות SQL שונות
- בחירה מרובה עם 4 אפשרויות
- מילים רנדומליות בכל משחק
- משוב מיידי על תשובות
- חישוב ציון (10 נקודות לכל תשובה נכונה)

### 3. טבלת מובילים (`/games/sql-english/leaderboard`)
- Top 10 שחקנים
- גביעים למקומות 1,2,3 (🥇🥈🥉)
- הצגת ציון, תשובות נכונות ואחוז הצלחה

### 4. היסטוריית משחקים (`/games/sql-english/history`)
- חיפוש לפי שם משתמש
- סטטיסטיקות מפורטות
- כל המשחקים הקודמים עם תאריך ושעה
- סימון הציון הגבוה ביותר

---

## טיפים למשתמשים:
- אין צורך להירשם - פשוט בחר שם משתמש
- שם המשתמש נשמר רק למעקב אחר ציונים
- כדאי לזכור את שם המשתמש כדי לראות את ההיסטוריה
- אפשר לשחק כמה פעמים שרוצים
